toc:
  - name: Sotez
    children:
      # - _provider
      # - _network
      # - _defaultFee
      # - _chain
      # - _localForge
      # - _validateLocalForge
      # - _counters
      # - _debugMode
      - setProvider
      - importKey
      - importLedger
      - query
      - account
      - getBalance
      - getDelegate
      - getManager
      - getCounter
      - getBaker
      - getHeader
      - getHead
      - getHeadHash
      - getBallotList
      - getProposals
      - getBallots
      - getListings
      - getCurrentProposal
      - getCurrentPeriod
      - getCurrentQuorum
      - awaitOperation
      - call
      - sendOperation
      - inject
      - silentInject
      - transfer
      - activate
      - originate
      - setDelegate
      - registerDelegate
      - typecheckCode
      - packData
      - typecheckData
      - runCode

  - name: crypto
    children:
      - extractKeys
      - generateMnemonic
      - checkAddress
      - generateKeys
      - sign
      - verify

  - name: utility
    children:
      - b582int
      - totez
      - mutez
      - b58cencode
      - b58cdecode
      - buf2hex
      - hex2buf
      - hexNonce
      - mergebuf
      # - sexp2mic
      # - mic2arr
      # - ml2mic
      # - formatMoney

  - name: forge
    children:
      - forge
      - decodeRawBytes
      - encodeRawBytes
      - toBytesInt32
      - toBytesInt32Hex
      - bool
      - script
      - parameters
      - publicKeyHash
      - address
      - zarith
      - publicKey
      - op

  - name: ledger
    children:
      - getAddress
      - signOperation
      - getVersion

  - name: key
    children:
      - publicKey
      - secretKey
      - publicKeyHash
      - sign
      - verify
